// This file was generated by prisma-field-encryption.

import type { PrismaClient, members } from '@prisma/client'
import {
  ProgressReportCallback,
  defaultProgressReport,
  visitRecords
} from 'prisma-field-encryption/dist/generator/runtime'

type Cursor = members['id']

export async function migrate(
  client: PrismaClient,
  reportProgress: ProgressReportCallback = defaultProgressReport
): Promise<number> {
  return visitRecords<PrismaClient, Cursor>({
    modelName: 'members',
    client,
    getTotalCount: client.members.count,
    migrateRecord,
    reportProgress,
  })
}

async function migrateRecord(client: PrismaClient, cursor: Cursor | undefined) {
  return await client.$transaction(async tx => {
    const record = await tx.members.findFirst({
      take: 1,
      skip: cursor === undefined ? undefined : 1,
      ...(cursor === undefined
        ? {}
        : {
            cursor: {
              id: cursor
            }
          }),
      orderBy: {
        id: 'asc'
      },
      select: {
        id: true,
        name: true,
        notes: true
      }
    })
    if (!record) {
      return cursor
    }
    await tx.members.update({
      where: {
        id: record.id
      },
      data: {
        name: record.name,
        notes: record.notes
      }
    })
    return record.id
  })
}

/**
 * Internal model:
 * {
 *   "cursor": "id",
 *   "fields": {
 *     "name": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     },
 *     "notes": {
 *       "encrypt": true,
 *       "strictDecryption": false
 *     }
 *   },
 *   "connections": {
 *     "vault": {
 *       "modelName": "vaults",
 *       "isList": false
 *     },
 *     "accounts": {
 *       "modelName": "accounts",
 *       "isList": true
 *     },
 *     "expenses": {
 *       "modelName": "expenses",
 *       "isList": true
 *     },
 *     "income": {
 *       "modelName": "income",
 *       "isList": true
 *     },
 *     "subscriptions": {
 *       "modelName": "subscriptions",
 *       "isList": true
 *     },
 *     "investments": {
 *       "modelName": "investments",
 *       "isList": true
 *     }
 *   }
 * }
 */
